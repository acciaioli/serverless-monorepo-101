name: Continuous Integration

on: push

jobs:

###  BACKEND TESTS  ###
  test-backend:
    defaults:
      run:
        working-directory: backend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-go@v1
        with:
          go-version: 1.14.x
      - uses: actions/checkout@v2
      # service tests
      - run: make test -s -C demo-service
      - run: make compile -s -C demo-service

###  FRONTEND TESTS  ###
  test-frontend:
    defaults:
      run:
        working-directory: frontend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: '12'
      - run: npm install
      - run: npm run lint

###  BACKEND BUILDS  ###
  build-backend:
    needs: test-backend
    if: github.ref == 'refs/heads/master'
    defaults:
      run:
        working-directory: backend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-go@v1
        with:
          go-version: 1.14.x
      - uses: actions/checkout@v2
      - run: echo "go run internal/deployment/main.go"
